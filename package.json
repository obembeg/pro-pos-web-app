{
    "name": "laundry-business-manager",
    "version": "1.0.0",
    "description": "Laundry Business Manager - Mobile App",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "cap:init": "npx cap init",
        "cap:sync": "npx cap sync",
        "cap:copy": "npx cap copy",
        "cap:open:android": "npx cap open android",
        "cap:open:ios": "npx cap open ios",
        "build": "if not exist www mkdir www && copy index.html www\\index.html && copy manifest.json www\\manifest.json && copy sw.js www\\sw.js && copy icon.png www\\icon.png && xcopy /E /I /Y downloads www\\downloads"
    },
    "keywords": [],
    "author": "OG",
    "license": "ISC",
    "dependencies": {
        "@capacitor/android": "^6.0.0",
        "@capacitor/app": "^6.0.0",
        "@capacitor/core": "^6.0.0",
        "@capacitor/filesystem": "^6.0.0",
        "@capacitor/haptics": "^6.0.0",
        "@capacitor/keyboard": "^6.0.0",
        "@capacitor/share": "^6.0.0",
        "@capacitor/status-bar": "^6.0.0"
    },
    "devDependencies": {
        "@capacitor/cli": "^6.0.0"
    }
}